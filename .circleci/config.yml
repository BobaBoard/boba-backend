version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - checkout
      - setup_remote_docker
      - node/with-cache:
          steps:
            - run: env > .env
            - run: npm install
            - run: npm run test-docker
workflows:
  build-and-test:
    jobs:
      - build-and-test
