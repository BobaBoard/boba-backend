version: 2.1
jobs:
  build-and-test:
    executor:
      machine:
        image: ubuntu-1604:201903-01
    steps:
      - checkout
      - setup_remote_docker
      - run: env > .env
      - run: sudo apt-get install postgresql-client
      - run: npm install
      - run: npm run test-docker
workflows:
  build-and-test:
    jobs:
      - build-and-test
