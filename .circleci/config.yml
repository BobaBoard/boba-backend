version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    steps:
      - checkout
      - node/with-cache:
          steps:
            - setup_remote_docker
            - run: env > .env
            - run: npm install
            - run: npm run test-docker
workflows:
  build-and-test:
    jobs:
      - build-and-test
