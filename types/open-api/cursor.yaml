components:
  schemas:
    Cursor:
      type: object
      properties:
        next:
          description: Pagination link pointing to the next page.
          anyOf:
            - type: string
              format: uri
            - type: "null"
      required:
        - next
