components:
  schemas:
    Cursor:
      type: object
      properties:
        next:
          description: Pagination link pointing to the next page.
          anyOf:
            - type: string
              # TODO: this should return the whole url to the next page
              # format: uri-reference
            - type: "null"
      required:
        - next
