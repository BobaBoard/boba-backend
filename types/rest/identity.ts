export interface Role {
  id: string;
  name: string;
  color?: string;
  accessory?: string;
  avatar_url: string;
}

export interface Accessory {
  id: string;
  name: string;
  accessory: string;
}
